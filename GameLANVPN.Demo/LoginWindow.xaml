<Window x:Class="GameLANVPN.Demo.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GameLAN VPN"
        Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Fantasy Dragon Theme -->
        <LinearGradientBrush x:Key="DragonGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#8B0000" Offset="0"/>
            <GradientStop Color="#DC143C" Offset="0.5"/>
            <GradientStop Color="#FF4500" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="GoldGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#FFD700" Offset="0"/>
            <GradientStop Color="#B8860B" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1A0D0D" Offset="0"/>
            <GradientStop Color="#2D1B1B" Offset="0.3"/>
            <GradientStop Color="#3D2828" Offset="0.7"/>
            <GradientStop Color="#1A0D0D" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="DragonButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DragonGradient}"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#FFD700"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#A0522D" Offset="0"/>
                                            <GradientStop Color="#CD853F" Offset="0.5"/>
                                            <GradientStop Color="#FF6347" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="#FFF8DC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DragonTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2D1B1B"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="BorderBrush" Value="#8B4513"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FFD700"/>
                                <Setter Property="Background" Value="#3D2828"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DragonPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#2D1B1B"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="BorderBrush" Value="#8B4513"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                               BorderBrush="{TemplateBinding BorderBrush}"
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FFD700"/>
                                <Setter Property="Background" Value="#3D2828"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom ComboBox Style for Dark Theme -->
        <Style x:Key="DragonComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2D1B1B"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="BorderBrush" Value="#8B4513"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        Focusable="False"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                               BorderBrush="{TemplateBinding BorderBrush}"
                                               BorderThickness="{TemplateBinding BorderThickness}"
                                               CornerRadius="5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                     Data="M 0 0 L 4 4 L 8 0 Z" Fill="#FFD700"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="8,3,23,3"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                   Style="{x:Null}"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Margin="3,3,23,3"
                                   Focusable="True"
                                   Background="Transparent"
                                   Foreground="{TemplateBinding Foreground}"
                                   Visibility="Hidden"
                                   IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True"
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                          Background="#2D1B1B"
                                          BorderThickness="2"
                                          BorderBrush="#8B4513"
                                          CornerRadius="5">
                                        <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FFD700"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#FFD700"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom ComboBoxItem Style for Dark Theme -->
        <Style x:Key="DragonComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter Property="Background" Value="#3D2828"/>
                                <Setter Property="Foreground" Value="#FFF8DC"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#8B4513"/>
                                <Setter Property="Foreground" Value="#FFD700"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" BorderThickness="3" BorderBrush="{StaticResource GoldGradient}">
        <Grid Background="{StaticResource BackgroundGradient}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Language Selector and Close Button -->
            <StackPanel Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top"
                       Orientation="Horizontal" Margin="15,15,15,0">

                <!-- Language Selector -->
                <ComboBox x:Name="LanguageSelector" Width="120" Height="30" Margin="0,0,10,0"
                         Style="{StaticResource DragonComboBox}"
                         SelectionChanged="LanguageSelector_SelectionChanged">
                    <ComboBoxItem Content="🇹🇭 ไทย" Tag="th-TH" IsSelected="True" Style="{StaticResource DragonComboBoxItem}"/>
                    <ComboBoxItem Content="🇺🇸 English" Tag="en-US" Style="{StaticResource DragonComboBoxItem}"/>
                </ComboBox>

                <!-- Close Button -->
                <Button HorizontalAlignment="Right" VerticalAlignment="Top"
                       Width="30" Height="30"
                       Background="Transparent" BorderThickness="0"
                       Foreground="#FFD700" FontSize="18" FontWeight="Bold"
                       Content="✕" Cursor="Hand" Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="15">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#33FFD700"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
            </StackPanel>

            <!-- Main Content -->
            <StackPanel Grid.Row="1" Margin="30,10,30,10" HorizontalAlignment="Center">

                <!-- Dragon Logo -->
                <Border Width="100" Height="100"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,20">
                    <Canvas Width="100" Height="100">
                        <!-- Dragon Silhouette -->
                        <Ellipse Width="80" Height="80" Canvas.Left="10" Canvas.Top="10"
                                Fill="{StaticResource DragonGradient}" Opacity="0.8"/>

                        <!-- Dragon Head -->
                        <Path Canvas.Left="30" Canvas.Top="25" Fill="{StaticResource GoldGradient}"
                              Data="M20,25 Q30,15 40,25 Q50,20 53,30 Q50,40 40,35 Q30,43 20,35 Q17,30 20,25 Z"/>

                        <!-- Dragon Eyes -->
                        <Ellipse Width="5" Height="5" Canvas.Left="35" Canvas.Top="32" Fill="#FF0000"/>
                        <Ellipse Width="5" Height="5" Canvas.Left="48" Canvas.Top="32" Fill="#FF0000"/>

                        <!-- Dragon Wings -->
                        <Path Canvas.Left="15" Canvas.Top="40" Fill="{StaticResource DragonGradient}" Opacity="0.7"
                              Data="M0,15 Q15,0 30,15 Q22,30 15,26 Q7,22 0,15 Z"/>
                        <Path Canvas.Left="65" Canvas.Top="40" Fill="{StaticResource DragonGradient}" Opacity="0.7"
                              Data="M30,15 Q15,0 0,15 Q7,22 15,26 Q22,30 30,15 Z"/>

                        <!-- Glow Effect -->
                        <Ellipse Width="95" Height="95" Canvas.Left="2.5" Canvas.Top="2.5"
                                Fill="Transparent" Stroke="#FFD700" StrokeThickness="2" Opacity="0.5"/>
                    </Canvas>
                    <Border.Effect>
                        <DropShadowEffect Color="#FFD700" BlurRadius="25" ShadowDepth="0" Opacity="0.7"/>
                    </Border.Effect>
                </Border>

                <!-- Title -->
                <TextBlock Text="{Binding LoginTitle}"
                          FontSize="28" FontWeight="Bold"
                          Foreground="{StaticResource GoldGradient}"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,10"/>

                <TextBlock Text="{Binding LoginSubtitle}"
                          FontSize="16" FontStyle="Italic"
                          Foreground="#CD853F"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20"/>

                <!-- Status Message -->
                <TextBlock x:Name="StatusMessage" Text=""
                          FontSize="12" FontWeight="Bold"
                          Foreground="#FF0000"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,20"
                          TextWrapping="Wrap"/>

                <!-- Login Form -->
                <StackPanel Width="300">
                    <!-- Username -->
                    <TextBlock Text="{Binding UsernameLabel}" FontSize="14" FontWeight="Bold"
                              Foreground="#FFD700" Margin="0,0,0,8"/>
                    <TextBox x:Name="UsernameTextBox" Style="{StaticResource DragonTextBox}"
                            Text="test" Margin="0,0,0,20"/>

                    <!-- Password -->
                    <TextBlock Text="{Binding PasswordLabel}" FontSize="14" FontWeight="Bold"
                              Foreground="#FFD700" Margin="0,0,0,8"/>
                    <PasswordBox x:Name="PasswordBox" Style="{StaticResource DragonPasswordBox}"
                                Password="test" Margin="0,0,0,30"/>

                    <!-- Login Button -->
                    <Button x:Name="LoginBtn"
                           Content="🚀 เข้าสู่ระบบ 🚀"
                           Click="LoginButton_Click"
                           MouseDown="LoginButton_MouseDown"
                           Margin="0,0,0,20"
                           Background="Red"
                           Foreground="Gold"
                           FontWeight="Bold"
                           FontSize="16"
                           Padding="20,12"
                           Height="45"
                           IsDefault="True"
                           TabIndex="2"/>
                </StackPanel>

            </StackPanel>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#1A0D0D" Opacity="0.8"
                   CornerRadius="0,0,17,17" Padding="20,15">
                <Grid>
                    <TextBlock Text="{Binding FooterText}"
                              FontSize="12" FontStyle="Italic"
                              Foreground="#CD853F"
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="v0.2.0" FontSize="10"
                              Foreground="#8B4513"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Bottom"/>
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>